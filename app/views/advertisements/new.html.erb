<h1>Create a New Advertisement</h1>
<div class="row">
  <div class="col-md-4">
    <h4>Guidelines for advertisements</h4>
    <ul>
      <li>Make sure it's not junk.</li>
      <li>The price is a whole number. No one like change.</li>
      <li>Again - make sure it's not junk.</li>
    </ul>
  </div>

  <div class="col-md-8">
    <%= form_for @advertisement do |ad| %>
      <div class="form-group">
        <%= ad.label :title %>
        <%= ad.text_field :title, class:'form-control', placeholder:'What are you selling?' %>
      </div>
      <div class="form-group">
        <%= ad.label :copy %>
        <%= ad.text_area :copy, rows: 8, class:'form-control', placeholder:'Describe it a little ...' %>
      </div>
      <div class="form-group">
        <%= ad.label :price %>
        <%= ad.text_field :price, rows: 8, class:'form-control', placeholder:'What do you want for it?' %>
      </div>
      <div class="form-group">
        <%= ad.submit "Save", class:'btn btn-info' %>
      </div>
    <% end %>
  </div>
</div>
